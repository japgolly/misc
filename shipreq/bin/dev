#!/bin/bash
cd "$(dirname "$(readlink -e "$0")")" || exit 1

for arg in "$@"; do
  if [ "$arg" = test ]; then
    echo "You've passed 'test' as an argument to bin/dev"
    echo "Pretty sure you mean: bin/env $@"
    exit 0
  fi
done

exec ./env dev "$@"
